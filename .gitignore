.nuxt
.output
node_modules
.env
.DS_Store
app.log

# Large local brand source (cropped logo is in assets/img/logo.png)
AI Elegance logo design.png

# Staged uploads (video start frames, etc.)
.data/

# PocketBase binary and local data (keep pb_migrations/*.js in git)
pocketbase/pb_data/
pocketbase/pocketbase
pocketbase/pocketbase.exe

