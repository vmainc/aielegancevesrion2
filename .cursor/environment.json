{
  "name": "AI Film Studio",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "pocketbase",
      "command": "cd pocketbase && ./pocketbase serve --http=127.0.0.1:8090"
    },
    {
      "name": "nuxt",
      "command": "until curl -sf http://127.0.0.1:8090/api/health >/dev/null 2>&1; do sleep 1; done; npm run dev"
    }
  ],
  "ports": [3000, 8090]
}
